import React from "react";
import styled from "styled-components";

const MainApp = () => {
  return <Container>MainApp Screen</Container>;
};

export default MainApp;

const Container = styled.div`
  background-color: green;
  flex: 1;
`;
